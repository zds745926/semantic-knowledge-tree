{
  "name": "非对称加密",
  "node_id": "asymmetric_enc_v4",
  "parent_id": "crypto_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "密码学",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "非对称加密: RSA/ECC. Public key encryption with private key decryption. Digital signatures. Here are the corresponding concepts in Python using cryptography library examples: For RSA: ```python from cryptography.hazmat.primitives.asymmetric import rsa, padding from cryptography.hazmat.primitives import hashes # Generate a new RSA private key private_key = rsa.generate_private_key( public_exponent=65537, key_size=2048, ) # Extract the corresponding public key public_key = private_key.public_key() # Encrypt data with the public key ciphertext = public_key.encrypt( b\"Secret message\", padding.OAEP( mgf=padding.MGF1(algorithm=hashes.SHA256()), algorithm=hashes.SHA256(), label=None, ) ) # Decrypt data with the private key plaintext = private_key.decrypt( ciphertext, padding.OAEP( mgf=padding.MGF1(algorithm=hashes.SHA256()), algorithm=hashes.SHA256(), label=None, ) ) ``` For ECC: ```python from cryptography.hazmat.primitives.asymmetric import ec # Generate a new EC private key private_key = ec.generate_private_key(ec.SECP384R1()) # Extract the corresponding public key public_key = private_key.public_key() # Sign data with the private key signature = private_key.sign( b\"Message to sign\", ec.ECDSA(hashes.SHA256()) ) # Verify the signature with the public key public_key.verify( signature, b\"Message to sign\", ec.ECDSA(hashes.SHA256()) ) ``` These examples demonstrate basic usage of RSA and ECC for encryption, decryption, and digital signatures."
  },
  "data_pointers": [
    {
      "title": "非对称加密",
      "uri": "knowledge://asymmetric_enc_v4",
      "content_preview": "非对称加密: RSA/ECC. Public key encryption with private key decryption. Digital signatures. Here are the corresponding concepts in Python using cryptography library examples: For RSA: ```python from cryptography.hazmat.primitives.asymmetric import rsa, padding from cryptography.hazmat.primitives import hashes # Generate a new RSA private key private_key = rsa.generate_private_key( public_exponent=65537, key_size=2048, ) # Extract the corresponding public key public_key = private_key.public_key() # Encrypt data with the public key ciphertext = public_key.encrypt( b\"Secret message\", padding.OAEP( mgf=padding.MGF1(algorithm=hashes.SHA256()), algorithm=hashes.SHA256(), label=None, ) ) # Decrypt data with the private key plaintext = private_key.decrypt( ciphertext, padding.OAEP( mgf=padding.MGF1(algorithm=hashes.SHA256()), algorithm=hashes.SHA256(), label=None, ) ) ``` For ECC: ```python from cryptography.hazmat.primitives.asymmetric import ec # Generate a new EC private key private_key = ec.generate_private_key(ec.SECP384R1()) # Extract the corresponding public key public_key = private_key.public_key() # Sign data with the private key signature = private_key.sign( b\"Message to sign\", ec.ECDSA(hashes.SHA256()) ) # Verify the signature with the public key public_key.verify( signature, b\"Message to sign\", ec.ECDSA(hashes.SHA256()) ) ``` These examples demonstrate basic usage of RSA and ECC for encryption, decryption, and digital signatures."
    }
  ],
  "related_nodes": []
}