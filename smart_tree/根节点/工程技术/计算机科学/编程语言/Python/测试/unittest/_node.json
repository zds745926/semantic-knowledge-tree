{
  "name": "unittest",
  "node_id": "py_unittest_v4",
  "parent_id": "py_testing_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "测试",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "unittest: Built-in testing framework. `TestCase` class. `setUp`/`tearDown`. `assertEqual`/`assertRaises`. Mock integration. Here's a brief explanation with Python code examples: - **`TestCase` class**: This is the base class for all test cases in Python’s unittest module. ```python import unittest class TestExample(unittest.TestCase): def setUp(self): # Setup method to run before each test self.value = 10 def tearDown(self): # Teardown method to run after each test del self.value def test_addition(self): result = self.value + 5 self.assertEqual(result, 15) # Assert that the result is as expected def test_subtraction(self): with self.assertRaises(TypeError): # Assert that a TypeError is raised result = self.value - 'a' ``` - **`setUp`/`tearDown`**: These methods are used to set up and tear down resources before and after each test, respectively. - **`assertEqual`/`assertRaises`**: `assertEqual` checks if two values are equal. `assertRaises` is used to check that a specific exception is raised when a piece of code is executed. ```python import unittest class TestExample(unittest.TestCase): def test_addition(self): self.assertEqual(10 + 5, 15) # Assert that the addition result is correct def test_division_by_zero(self): with self.assertRaises(ZeroDivisionError): # Assert that a ZeroDivisionError is raised result = 10 / 0 ``` - **Mock integration**: Mocks are used to replace parts of your system under test, allowing you to isolate the code being tested from external dependencies. ```python import unittest.mock class TestExample(unittest.TestCase): def test_function_with_side_effect(self): with unittest.mock.patch('module.function') as mock_function: mock_function.return_value = 'mocked result' self.assertEqual(module.function(), 'mocked result') ``` These examples should give you a clear understanding of how to use these concepts in Python testing."
  },
  "data_pointers": [
    {
      "title": "unittest",
      "uri": "knowledge://py_unittest_v4",
      "content_preview": "unittest: Built-in testing framework. `TestCase` class. `setUp`/`tearDown`. `assertEqual`/`assertRaises`. Mock integration. Here's a brief explanation with Python code examples: - **`TestCase` class**: This is the base class for all test cases in Python’s unittest module. ```python import unittest class TestExample(unittest.TestCase): def setUp(self): # Setup method to run before each test self.value = 10 def tearDown(self): # Teardown method to run after each test del self.value def test_addition(self): result = self.value + 5 self.assertEqual(result, 15) # Assert that the result is as expected def test_subtraction(self): with self.assertRaises(TypeError): # Assert that a TypeError is raised result = self.value - 'a' ``` - **`setUp`/`tearDown`**: These methods are used to set up and tear down resources before and after each test, respectively. - **`assertEqual`/`assertRaises`**: `assertEqual` checks if two values are equal. `assertRaises` is used to check that a specific exception is raised when a piece of code is executed. ```python import unittest class TestExample(unittest.TestCase): def test_addition(self): self.assertEqual(10 + 5, 15) # Assert that the addition result is correct def test_division_by_zero(self): with self.assertRaises(ZeroDivisionError): # Assert that a ZeroDivisionError is raised result = 10 / 0 ``` - **Mock integration**: Mocks are used to replace parts of your system under test, allowing you to isolate the code being tested from external dependencies. ```python import unittest.mock class TestExample(unittest.TestCase): def test_function_with_side_effect(self): with unittest.mock.patch('module.function') as mock_function: mock_function.return_value = 'mocked result' self.assertEqual(module.function(), 'mocked result') ``` These examples should give you a clear understanding of how to use these concepts in Python testing."
    }
  ],
  "related_nodes": [
    "py_mock_v4"
  ]
}