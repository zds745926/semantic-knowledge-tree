{
  "name": "Mock 模拟",
  "node_id": "py_mock_v4",
  "parent_id": "py_testing_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "测试",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "Mock 模拟: unittest.mock replaces external dependencies. It includes `Mock`, `MagicMock`, and `patch`. `side_effect` and `return_value` are used to control behavior. Here's a brief explanation with Python code examples: - **unittest.mock**: This module provides tools for replacing parts of your system under test, which can be particularly useful when dealing with external dependencies like file systems or network calls. - **Mock** and **MagicMock**: These classes allow you to create mock objects that mimic the behavior of real objects. `MagicMock` is a subclass of `Mock` with additional magic methods. - **patch**: This function allows you to replace parts of the system under test (or any other module) with a \"stub\" version during tests, which can be particularly useful for isolating your code from external dependencies. - **side_effect** and **return_value**: These attributes control how mock objects behave. `return_value` sets what value is returned when a method or function is called on the mock object. `side_effect` allows you to specify a callable that will be invoked instead of the method being called, which can simulate side effects like raising exceptions. Here's an example: ```python from unittest.mock import patch def test_example(): # Using Mock to replace a function call with patch('module.function_to_patch') as mock_function: mock_function.return_value = 'mocked result' # Call the function that uses the patched function result = module.some_function() assert result == 'mocked result' # Example of using side_effect to simulate a side effect def test_side_effect(): with patch('module.function_to_patch') as mock_function: def side_effect(*args, **kwargs): if args[0] == 'error_case': raise ValueError(\"Simulated error\") return \"normal result\" mock_function.side_effect = side_effect # Test the function that calls the patched function assert module.some_function('normal_case') == 'normal result' with pytest.raises(ValueError): module.some_function('error_case') ``` In this example, `patch` is used to replace a function in another module during testing. The `return_value` attribute sets what value should be returned when the mocked function is called. The `side_effect` attribute allows you to define a callable that will be invoked instead of the method being called on the mock object, simulating side effects like raising exceptions."
  },
  "data_pointers": [
    {
      "title": "Mock 模拟",
      "uri": "knowledge://py_mock_v4",
      "content_preview": "Mock 模拟: unittest.mock replaces external dependencies. It includes `Mock`, `MagicMock`, and `patch`. `side_effect` and `return_value` are used to control behavior. Here's a brief explanation with Python code examples: - **unittest.mock**: This module provides tools for replacing parts of your system under test, which can be particularly useful when dealing with external dependencies like file systems or network calls. - **Mock** and **MagicMock**: These classes allow you to create mock objects that mimic the behavior of real objects. `MagicMock` is a subclass of `Mock` with additional magic methods. - **patch**: This function allows you to replace parts of the system under test (or any other module) with a \"stub\" version during tests, which can be particularly useful for isolating your code from external dependencies. - **side_effect** and **return_value**: These attributes control how mock objects behave. `return_value` sets what value is returned when a method or function is called on the mock object. `side_effect` allows you to specify a callable that will be invoked instead of the method being called, which can simulate side effects like raising exceptions. Here's an example: ```python from unittest.mock import patch def test_example(): # Using Mock to replace a function call with patch('module.function_to_patch') as mock_function: mock_function.return_value = 'mocked result' # Call the function that uses the patched function result = module.some_function() assert result == 'mocked result' # Example of using side_effect to simulate a side effect def test_side_effect(): with patch('module.function_to_patch') as mock_function: def side_effect(*args, **kwargs): if args[0] == 'error_case': raise ValueError(\"Simulated error\") return \"normal result\" mock_function.side_effect = side_effect # Test the function that calls the patched function assert module.some_function('normal_case') == 'normal result' with pytest.raises(ValueError): module.some_function('error_case') ``` In this example, `patch` is used to replace a function in another module during testing. The `return_value` attribute sets what value should be returned when the mocked function is called. The `side_effect` attribute allows you to define a callable that will be invoked instead of the method being called on the mock object, simulating side effects like raising exceptions."
    }
  ],
  "related_nodes": []
}