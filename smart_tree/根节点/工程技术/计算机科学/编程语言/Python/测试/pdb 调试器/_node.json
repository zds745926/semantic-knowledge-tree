{
  "name": "pdb 调试器",
  "node_id": "py_pdb_v4",
  "parent_id": "py_testing_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "测试",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "pdb 调试器: The command `python -m pdb script.py` starts the Python debugger (pdb) on the specified script. Here are the key commands you can use within pdb: - `l` (list): Lists the source code for the current scope. - `n` (next): Execute the next line of code. - `s` (step): Step into function calls. - `c` (continue): Continue execution until a breakpoint is hit or the program terminates. - `p` (print): Print the value of an expression. - `q` (quit): Exit the debugger. Here's a brief Python code example to illustrate using pdb: ```python import pdb def calculate(x, y): result = x + y pdb.set_trace() # This will pause execution here and enter pdb mode return result calculate(5, 3) ``` When you run this script with `python -m pdb script.py`, it will pause at the line containing `pdb.set_trace()` and allow you to use pdb commands like `l`, `n`, `s`, etc."
  },
  "data_pointers": [
    {
      "title": "pdb 调试器",
      "uri": "knowledge://py_pdb_v4",
      "content_preview": "pdb 调试器: The command `python -m pdb script.py` starts the Python debugger (pdb) on the specified script. Here are the key commands you can use within pdb: - `l` (list): Lists the source code for the current scope. - `n` (next): Execute the next line of code. - `s` (step): Step into function calls. - `c` (continue): Continue execution until a breakpoint is hit or the program terminates. - `p` (print): Print the value of an expression. - `q` (quit): Exit the debugger. Here's a brief Python code example to illustrate using pdb: ```python import pdb def calculate(x, y): result = x + y pdb.set_trace() # This will pause execution here and enter pdb mode return result calculate(5, 3) ``` When you run this script with `python -m pdb script.py`, it will pause at the line containing `pdb.set_trace()` and allow you to use pdb commands like `l`, `n`, `s`, etc."
    }
  ],
  "related_nodes": []
}