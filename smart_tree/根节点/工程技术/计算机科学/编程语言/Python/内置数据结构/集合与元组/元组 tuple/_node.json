{
  "name": "元组 tuple",
  "node_id": "py_tuple_v4",
  "parent_id": "py_set_v4",
  "level": 7,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "集合与元组",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "元组 tuple: A tuple is an immutable ordered collection. It can be created using (1, 2, 3). Tuples can be used as dictionary keys. Named tuples are a subclass of tuples with named fields. Here's a brief Python code example for creating and using a named tuple: ```python from collections import namedtuple # Define a named tuple called Point with two fields: x and y Point = namedtuple('Point', ['x', 'y']) # Create an instance of Point p = Point(1, 2) # Access the fields by name print(p.x) # Output: 1 print(p.y) # Output: 2 # Use a tuple as a dictionary key (since tuples are immutable) my_dict = {(1, 2): 'value'} print(my_dict[(1, 2)]) # Output: value ``` This example demonstrates creating a named tuple and accessing its fields by name. It also shows how a tuple can be used as a key in a dictionary."
  },
  "data_pointers": [
    {
      "title": "元组 tuple",
      "uri": "knowledge://py_tuple_v4",
      "content_preview": "元组 tuple: A tuple is an immutable ordered collection. It can be created using (1, 2, 3). Tuples can be used as dictionary keys. Named tuples are a subclass of tuples with named fields. Here's a brief Python code example for creating and using a named tuple: ```python from collections import namedtuple # Define a named tuple called Point with two fields: x and y Point = namedtuple('Point', ['x', 'y']) # Create an instance of Point p = Point(1, 2) # Access the fields by name print(p.x) # Output: 1 print(p.y) # Output: 2 # Use a tuple as a dictionary key (since tuples are immutable) my_dict = {(1, 2): 'value'} print(my_dict[(1, 2)]) # Output: value ``` This example demonstrates creating a named tuple and accessing its fields by name. It also shows how a tuple can be used as a key in a dictionary."
    }
  ],
  "related_nodes": []
}