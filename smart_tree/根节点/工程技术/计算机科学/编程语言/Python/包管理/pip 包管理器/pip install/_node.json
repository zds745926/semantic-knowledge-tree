{
  "name": "pip install",
  "node_id": "py_pip_install_v4",
  "parent_id": "py_pip_branch_v4",
  "level": 7,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "pip 包管理器",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "pip install: English translation: ``` pip install package. This installs a specific package. pip install -r requirements.txt. This command installs all packages listed in the 'requirements.txt' file. -U upgrades the package to the latest version. For example, pip install package==latest_version -U. -e makes the installation editable, allowing you to modify the source code directly. Example: ``` ```python # First, ensure you have a package installed with -e pip install -e /path/to/package # Then, you can make changes to the package's source files and they will be immediately available without needing to reinstall. ``` ```"
  },
  "data_pointers": [
    {
      "title": "pip install",
      "uri": "knowledge://py_pip_install_v4",
      "content_preview": "pip install: English translation: ``` pip install package. This installs a specific package. pip install -r requirements.txt. This command installs all packages listed in the 'requirements.txt' file. -U upgrades the package to the latest version. For example, pip install package==latest_version -U. -e makes the installation editable, allowing you to modify the source code directly. Example: ``` ```python # First, ensure you have a package installed with -e pip install -e /path/to/package # Then, you can make changes to the package's source files and they will be immediately available without needing to reinstall. ``` ```"
    }
  ],
  "related_nodes": [
    "py_requirements_v4"
  ]
}