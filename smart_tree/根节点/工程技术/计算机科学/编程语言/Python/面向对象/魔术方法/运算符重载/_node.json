{
  "name": "运算符重载",
  "node_id": "py_magic_ops_v4",
  "parent_id": "py_magic_v4",
  "level": 7,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "魔术方法",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "运算符重载: `__add__`, `__eq__`, `__lt__`, `__hash__`, `__call__`. Explanation of the terms and a brief Python code example for each: 1. **`__add__`**: This special method is used to define behavior when the `+` operator is used with an instance of your class. ```python class Vector: def __init__(self, x, y): self.x = x self.y = y def __add__(self, other): return Vector(self.x + other.x, self.y + other.y) v1 = Vector(1, 2) v2 = Vector(3, 4) print(v1 + v2) # Output: <__main__.Vector object at ...> ``` 2. **`__eq__`**: This special method is used to define behavior when the `==` operator is used with an instance of your class. ```python class Point: def __init__(self, x, y): self.x = x self.y = y def __eq__(self, other): return (self.x, self.y) == (other.x, other.y) p1 = Point(1, 2) p2 = Point(1, 2) print(p1 == p2) # Output: True ``` 3. **`__lt__`**: This special method is used to define behavior when the `<` operator is used with an instance of your class. ```python class Number: def __init__(self, value): self.value = value def __lt__(self, other): return self.value < other.value n1 = Number(5) n2 = Number(3) print(n1 < n2) # Output: False ``` 4. **`__hash__`**: This special method is used to define the hash value of an object, which is required for objects to be used as dictionary keys. ```python class MyObject: def __init__(self, key): self.key = key def __hash__(self): return hash(self.key) obj1 = MyObject('key1') d = {obj1: 'value'} print(d[obj1]) # Output: value ``` 5. **`__call__`**: This special method is used to make an instance of a class callable like a function. ```python class Counter: def __init__(self): self.count = 0 def __call__(self, increment=1): self.count += increment return self.count counter = Counter() print(counter(5)) # Output: 5 print(counter()) # Output: 6 (since count was incremented by 1) ``` These examples illustrate how these special methods can be implemented in Python to customize the behavior of your classes."
  },
  "data_pointers": [
    {
      "title": "运算符重载",
      "uri": "knowledge://py_magic_ops_v4",
      "content_preview": "运算符重载: `__add__`, `__eq__`, `__lt__`, `__hash__`, `__call__`. Explanation of the terms and a brief Python code example for each: 1. **`__add__`**: This special method is used to define behavior when the `+` operator is used with an instance of your class. ```python class Vector: def __init__(self, x, y): self.x = x self.y = y def __add__(self, other): return Vector(self.x + other.x, self.y + other.y) v1 = Vector(1, 2) v2 = Vector(3, 4) print(v1 + v2) # Output: <__main__.Vector object at ...> ``` 2. **`__eq__`**: This special method is used to define behavior when the `==` operator is used with an instance of your class. ```python class Point: def __init__(self, x, y): self.x = x self.y = y def __eq__(self, other): return (self.x, self.y) == (other.x, other.y) p1 = Point(1, 2) p2 = Point(1, 2) print(p1 == p2) # Output: True ``` 3. **`__lt__`**: This special method is used to define behavior when the `<` operator is used with an instance of your class. ```python class Number: def __init__(self, value): self.value = value def __lt__(self, other): return self.value < other.value n1 = Number(5) n2 = Number(3) print(n1 < n2) # Output: False ``` 4. **`__hash__`**: This special method is used to define the hash value of an object, which is required for objects to be used as dictionary keys. ```python class MyObject: def __init__(self, key): self.key = key def __hash__(self): return hash(self.key) obj1 = MyObject('key1') d = {obj1: 'value'} print(d[obj1]) # Output: value ``` 5. **`__call__`**: This special method is used to make an instance of a class callable like a function. ```python class Counter: def __init__(self): self.count = 0 def __call__(self, increment=1): self.count += increment return self.count counter = Counter() print(counter(5)) # Output: 5 print(counter()) # Output: 6 (since count was incremented by 1) ``` These examples illustrate how these special methods can be implemented in Python to customize the behavior of your classes."
    }
  ],
  "related_nodes": []
}