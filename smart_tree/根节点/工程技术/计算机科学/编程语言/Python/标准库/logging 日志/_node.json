{
  "name": "logging 日志",
  "node_id": "py_logging_v4",
  "parent_id": "py_stdlib_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "标准库",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "logging 日志: Logging Module: Logger/Handler/Formatter/Filter. Levels: DEBUG < INFO < WARNING < ERROR < CRITICAL. Here's a brief explanation of each component with a Python code example: - **Logger**: An instance of the logger is created to log messages at different levels. ```python import logging # Create a logger object logger = logging.getLogger('example_logger') logger.setLevel(logging.DEBUG) # Set the minimum level for this logger # Example usage: logger.debug(\"This is a debug message\") ``` - **Handler**: A handler specifies where to send the log messages, such as to the console or a file. ```python # Create a console handler and set its level to DEBUG ch = logging.StreamHandler() ch.setLevel(logging.DEBUG) # Add the handler to the logger logger.addHandler(ch) # Example usage: logger.debug(\"This message will be sent to the console\") ``` - **Formatter**: A formatter defines the format of the log messages. ```python # Create a formatter and set its format formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s') # Add the formatter to the handler ch.setFormatter(formatter) # Example usage: logger.debug(\"This message will be formatted with timestamp, name, level, and message\") ``` - **Filter**: A filter can be used to control which log messages are actually logged. ```python # Create a filter that only allows messages with level CRITICAL or higher class CriticalFilter(logging.Filter): def filter(self, record): return record.levelno >= logging.CRITICAL # Add the filter to the logger logger.addFilter(CriticalFilter()) # Example usage: logger.debug(\"This message will not be logged\") logger.critical(\"This critical message will be logged\") ``` These examples illustrate how each component works together in a Python logging setup."
  },
  "data_pointers": [
    {
      "title": "logging 日志",
      "uri": "knowledge://py_logging_v4",
      "content_preview": "logging 日志: Logging Module: Logger/Handler/Formatter/Filter. Levels: DEBUG < INFO < WARNING < ERROR < CRITICAL. Here's a brief explanation of each component with a Python code example: - **Logger**: An instance of the logger is created to log messages at different levels. ```python import logging # Create a logger object logger = logging.getLogger('example_logger') logger.setLevel(logging.DEBUG) # Set the minimum level for this logger # Example usage: logger.debug(\"This is a debug message\") ``` - **Handler**: A handler specifies where to send the log messages, such as to the console or a file. ```python # Create a console handler and set its level to DEBUG ch = logging.StreamHandler() ch.setLevel(logging.DEBUG) # Add the handler to the logger logger.addHandler(ch) # Example usage: logger.debug(\"This message will be sent to the console\") ``` - **Formatter**: A formatter defines the format of the log messages. ```python # Create a formatter and set its format formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s') # Add the formatter to the handler ch.setFormatter(formatter) # Example usage: logger.debug(\"This message will be formatted with timestamp, name, level, and message\") ``` - **Filter**: A filter can be used to control which log messages are actually logged. ```python # Create a filter that only allows messages with level CRITICAL or higher class CriticalFilter(logging.Filter): def filter(self, record): return record.levelno >= logging.CRITICAL # Add the filter to the logger logger.addFilter(CriticalFilter()) # Example usage: logger.debug(\"This message will not be logged\") logger.critical(\"This critical message will be logged\") ``` These examples illustrate how each component works together in a Python logging setup."
    }
  ],
  "related_nodes": []
}