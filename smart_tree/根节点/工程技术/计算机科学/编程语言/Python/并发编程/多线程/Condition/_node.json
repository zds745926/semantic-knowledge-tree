{
  "name": "Condition",
  "node_id": "py_condition_v4",
  "parent_id": "py_thread_v4",
  "level": 7,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "多线程",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "Condition: `threading.Condition`: A condition variable. It is used for waiting and notification between threads. The `wait()` method causes the current thread to wait until it is notified, while `notify()` or `notify_all()` wakes up one or all waiting threads respectively. This pattern is often used in producer-consumer scenarios. Python code example: ```python import threading class ProducerConsumer: def __init__(self): self.condition = threading.Condition() self.buffer = [] def produce(self, item): with self.condition: # Wait until the buffer has space for a new item while len(self.buffer) == 10: self.condition.wait() # Add the item to the buffer self.buffer.append(item) print(f\"Produced: {item}\") # Notify one waiting consumer thread self.condition.notify() def consume(self): with self.condition: # Wait until there is an item in the buffer while not self.buffer: self.condition.wait() # Remove and return the first item from the buffer item = self.buffer.pop(0) print(f\"Consumed: {item}\") # Notify one waiting producer thread self.condition.notify() # Example usage producer_consumer = ProducerConsumer() producer_thread = threading.Thread(target=producer_consumer.produce, args=(1,)) consumer_thread = threading.Thread(target=producer_consumer.consume) producer_thread.start() consumer_thread.start() ``` This example demonstrates the use of `threading.Condition` in a producer-consumer scenario where both threads wait and notify each other based on the state of the shared buffer."
  },
  "data_pointers": [
    {
      "title": "Condition",
      "uri": "knowledge://py_condition_v4",
      "content_preview": "Condition: `threading.Condition`: A condition variable. It is used for waiting and notification between threads. The `wait()` method causes the current thread to wait until it is notified, while `notify()` or `notify_all()` wakes up one or all waiting threads respectively. This pattern is often used in producer-consumer scenarios. Python code example: ```python import threading class ProducerConsumer: def __init__(self): self.condition = threading.Condition() self.buffer = [] def produce(self, item): with self.condition: # Wait until the buffer has space for a new item while len(self.buffer) == 10: self.condition.wait() # Add the item to the buffer self.buffer.append(item) print(f\"Produced: {item}\") # Notify one waiting consumer thread self.condition.notify() def consume(self): with self.condition: # Wait until there is an item in the buffer while not self.buffer: self.condition.wait() # Remove and return the first item from the buffer item = self.buffer.pop(0) print(f\"Consumed: {item}\") # Notify one waiting producer thread self.condition.notify() # Example usage producer_consumer = ProducerConsumer() producer_thread = threading.Thread(target=producer_consumer.produce, args=(1,)) consumer_thread = threading.Thread(target=producer_consumer.consume) producer_thread.start() consumer_thread.start() ``` This example demonstrates the use of `threading.Condition` in a producer-consumer scenario where both threads wait and notify each other based on the state of the shared buffer."
    }
  ],
  "related_nodes": []
}