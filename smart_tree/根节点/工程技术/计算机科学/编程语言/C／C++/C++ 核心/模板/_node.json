{
  "name": "模板",
  "node_id": "cpp_templates_v4",
  "parent_id": "cpp_core_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "C++ 核心",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "模板: Template<typename T> Generic Template. Template Specialization. Compile-Time Metaprogramming. Here are the explanations with a brief Python code example for each concept: 1. **Generic Template (Template<typename T>)** This is a template that can work with any data type specified by the user. ```python def generic_function(data: T) -> T: return data * 2 # Example operation, assuming T supports multiplication print(generic_function(5)) # Output: 10 print(generic_function(\"hello\")) # Output: 'hellohello' ``` 2. **Template Specialization** This is a way to provide a specific implementation for a template when the type parameter matches certain criteria. ```python from typing import TypeVar, Generic T = TypeVar('T') class MyGenericClass(Generic[T]): def __init__(self, value: T): self.value = value # Specialization for int class MyIntClass(MyGenericClass[int]): def double_value(self) -> int: return self.value * 2 my_int_class = MyIntClass(5) print(my_int_class.double_value()) # Output: 10 ``` 3. **Compile-Time Metaprogramming** This involves writing code that manipulates other code at compile time, often using templates or similar mechanisms. ```python from typing import Type def template_specialization_example(template_type: Type[T]) -> T: if template_type is int: return 42 # Special case for int else: raise ValueError(\"Unsupported type\") print(template_specialization_example(int)) # Output: 42 ``` These examples illustrate the concepts in Python, which does not have native support for C++ templates but can simulate similar behavior using `typing` and other mechanisms."
  },
  "data_pointers": [
    {
      "title": "模板",
      "uri": "knowledge://cpp_templates_v4",
      "content_preview": "模板: Template<typename T> Generic Template. Template Specialization. Compile-Time Metaprogramming. Here are the explanations with a brief Python code example for each concept: 1. **Generic Template (Template<typename T>)** This is a template that can work with any data type specified by the user. ```python def generic_function(data: T) -> T: return data * 2 # Example operation, assuming T supports multiplication print(generic_function(5)) # Output: 10 print(generic_function(\"hello\")) # Output: 'hellohello' ``` 2. **Template Specialization** This is a way to provide a specific implementation for a template when the type parameter matches certain criteria. ```python from typing import TypeVar, Generic T = TypeVar('T') class MyGenericClass(Generic[T]): def __init__(self, value: T): self.value = value # Specialization for int class MyIntClass(MyGenericClass[int]): def double_value(self) -> int: return self.value * 2 my_int_class = MyIntClass(5) print(my_int_class.double_value()) # Output: 10 ``` 3. **Compile-Time Metaprogramming** This involves writing code that manipulates other code at compile time, often using templates or similar mechanisms. ```python from typing import Type def template_specialization_example(template_type: Type[T]) -> T: if template_type is int: return 42 # Special case for int else: raise ValueError(\"Unsupported type\") print(template_specialization_example(int)) # Output: 42 ``` These examples illustrate the concepts in Python, which does not have native support for C++ templates but can simulate similar behavior using `typing` and other mechanisms."
    }
  ],
  "related_nodes": []
}