{
  "name": "Bash 控制流",
  "node_id": "bash_control_v4",
  "parent_id": "shell_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "Shell 脚本",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "Bash 控制流: if/for/while/case. Conditional statements [[ ]]. Explanation of the technical terms and addition of a brief Python code example: - `if`, `for`, `while`, `case`: These are control flow structures in programming languages used to make decisions or repeat actions based on certain conditions. - `if` is used for conditional execution. - `for` and `while` are loop constructs that allow repeated execution of a block of code as long as a specified condition is true. - `case` is part of a switch-case statement, which allows you to execute different blocks of code based on the value of an expression. Here's a brief Python example for each: ```python # if statement temperature = 30 if temperature > 25: print(\"It's warm outside!\") # for loop fruits = [\"apple\", \"banana\", \"cherry\"] for fruit in fruits: print(fruit) # while loop count = 0 while count < 5: print(count) count += 1 # case statement (Python does not have a built-in switch-case, but you can simulate it with dictionaries) def switch_case(value): return { 'a': lambda: print(\"Case A\"), 'b': lambda: print(\"Case B\"), 'c': lambda: print(\"Case C\") }.get(value, lambda: print(\"Default\"))() switch_case('b') ``` This example covers the basic usage of `if`, `for`, and `while` in Python. The switch-case is simulated using a dictionary to map values to functions."
  },
  "data_pointers": [
    {
      "title": "Bash 控制流",
      "uri": "knowledge://bash_control_v4",
      "content_preview": "Bash 控制流: if/for/while/case. Conditional statements [[ ]]. Explanation of the technical terms and addition of a brief Python code example: - `if`, `for`, `while`, `case`: These are control flow structures in programming languages used to make decisions or repeat actions based on certain conditions. - `if` is used for conditional execution. - `for` and `while` are loop constructs that allow repeated execution of a block of code as long as a specified condition is true. - `case` is part of a switch-case statement, which allows you to execute different blocks of code based on the value of an expression. Here's a brief Python example for each: ```python # if statement temperature = 30 if temperature > 25: print(\"It's warm outside!\") # for loop fruits = [\"apple\", \"banana\", \"cherry\"] for fruit in fruits: print(fruit) # while loop count = 0 while count < 5: print(count) count += 1 # case statement (Python does not have a built-in switch-case, but you can simulate it with dictionaries) def switch_case(value): return { 'a': lambda: print(\"Case A\"), 'b': lambda: print(\"Case B\"), 'c': lambda: print(\"Case C\") }.get(value, lambda: print(\"Default\"))() switch_case('b') ``` This example covers the basic usage of `if`, `for`, and `while` in Python. The switch-case is simulated using a dictionary to map values to functions."
    }
  ],
  "related_nodes": []
}