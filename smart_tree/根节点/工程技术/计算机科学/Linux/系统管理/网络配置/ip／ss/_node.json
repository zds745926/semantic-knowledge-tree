{
  "name": "ip/ss",
  "node_id": "ip_ss_v4",
  "parent_id": "linux_net_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "网络配置",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "ip/ss: IP address/link/route. `ss -tuln` as an alternative to `netstat`. In the context of network utilities in Linux, `ip`, `ss`, and `netstat` are commonly used tools for displaying network connections, routing tables, interface statistics, etc. - **ip**: This is a command-line utility that can be used to configure network interfaces and display their status. It's more powerful and flexible than the older `ifconfig`. - **ss**: This stands for \"stream state\" and is a utility similar to `netstat` but generally faster and more efficient, especially when dealing with large amounts of data. Here’s an example of how you might use these commands: ### Using `ip` To display all network interfaces: ```sh ip addr show ``` To display link layer information: ```sh ip -d link show ``` To display routing tables: ```sh ip route show ``` ### Using `ss` To display all active TCP connections: ```python import subprocess def get_active_tcp_connections(): result = subprocess.run(['ss', 'tcp'], capture_output=True, text=True) return result.stdout print(get_active_tcp_connections()) ``` This Python function uses the `subprocess` module to run the `ss -t` command and captures its output. To display all active UDP connections: ```python def get_active_udp_connections(): result = subprocess.run(['ss', 'udp'], capture_output=True, text=True) return result.stdout print(get_active_udp_connections()) ``` These examples demonstrate how to use Python to interface with the `ss` command for network diagnostics."
  },
  "data_pointers": [
    {
      "title": "ip/ss",
      "uri": "knowledge://ip_ss_v4",
      "content_preview": "ip/ss: IP address/link/route. `ss -tuln` as an alternative to `netstat`. In the context of network utilities in Linux, `ip`, `ss`, and `netstat` are commonly used tools for displaying network connections, routing tables, interface statistics, etc. - **ip**: This is a command-line utility that can be used to configure network interfaces and display their status. It's more powerful and flexible than the older `ifconfig`. - **ss**: This stands for \"stream state\" and is a utility similar to `netstat` but generally faster and more efficient, especially when dealing with large amounts of data. Here’s an example of how you might use these commands: ### Using `ip` To display all network interfaces: ```sh ip addr show ``` To display link layer information: ```sh ip -d link show ``` To display routing tables: ```sh ip route show ``` ### Using `ss` To display all active TCP connections: ```python import subprocess def get_active_tcp_connections(): result = subprocess.run(['ss', 'tcp'], capture_output=True, text=True) return result.stdout print(get_active_tcp_connections()) ``` This Python function uses the `subprocess` module to run the `ss -t` command and captures its output. To display all active UDP connections: ```python def get_active_udp_connections(): result = subprocess.run(['ss', 'udp'], capture_output=True, text=True) return result.stdout print(get_active_udp_connections()) ``` These examples demonstrate how to use Python to interface with the `ss` command for network diagnostics."
    }
  ],
  "related_nodes": []
}