{
  "name": "优化器",
  "node_id": "optimizers_v4",
  "parent_id": "nn_basics_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "神经网络基础",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "优化器: SGD/Adam/RMSprop/AdamW. Learning rate scheduling. Here are brief descriptions and corresponding Python code examples for each optimizer: 1. **SGD (Stochastic Gradient Descent)** - A simple optimization algorithm that updates the model parameters based on the negative gradient of the loss function. ```python from torch.optim import SGD # Example usage with PyTorch optimizer = SGD(model.parameters(), lr=0.01) ``` 2. **Adam (Adaptive Moment Estimation)** - An adaptive learning rate optimization algorithm that computes individual adaptive learning rates for different parameters. ```python from torch.optim import Adam # Example usage with PyTorch optimizer = Adam(model.parameters(), lr=0.001) ``` 3. **RMSprop (Root Mean Square Propagation)** - An optimization algorithm that computes a moving average of squared gradients and uses it to normalize the gradient. ```python from torch.optim import RMSprop # Example usage with PyTorch optimizer = RMSprop(model.parameters(), lr=0.01, alpha=0.99) ``` 4. **AdamW (Adam with Weight Decay)** - A variant of Adam that includes weight decay directly in the optimization process. ```python from torch.optim import AdamW # Example usage with PyTorch optimizer = AdamW(model.parameters(), lr=0.001, weight_decay=0.01) ``` 5. **Learning Rate Scheduling** - A technique to adjust the learning rate during training to improve model performance. ```python from torch.optim.lr_scheduler import StepLR # Example usage with PyTorch scheduler = StepLR(optimizer, step_size=30, gamma=0.1) ``` These examples use PyTorch as the framework for illustration purposes."
  },
  "data_pointers": [
    {
      "title": "优化器",
      "uri": "knowledge://optimizers_v4",
      "content_preview": "优化器: SGD/Adam/RMSprop/AdamW. Learning rate scheduling. Here are brief descriptions and corresponding Python code examples for each optimizer: 1. **SGD (Stochastic Gradient Descent)** - A simple optimization algorithm that updates the model parameters based on the negative gradient of the loss function. ```python from torch.optim import SGD # Example usage with PyTorch optimizer = SGD(model.parameters(), lr=0.01) ``` 2. **Adam (Adaptive Moment Estimation)** - An adaptive learning rate optimization algorithm that computes individual adaptive learning rates for different parameters. ```python from torch.optim import Adam # Example usage with PyTorch optimizer = Adam(model.parameters(), lr=0.001) ``` 3. **RMSprop (Root Mean Square Propagation)** - An optimization algorithm that computes a moving average of squared gradients and uses it to normalize the gradient. ```python from torch.optim import RMSprop # Example usage with PyTorch optimizer = RMSprop(model.parameters(), lr=0.01, alpha=0.99) ``` 4. **AdamW (Adam with Weight Decay)** - A variant of Adam that includes weight decay directly in the optimization process. ```python from torch.optim import AdamW # Example usage with PyTorch optimizer = AdamW(model.parameters(), lr=0.001, weight_decay=0.01) ``` 5. **Learning Rate Scheduling** - A technique to adjust the learning rate during training to improve model performance. ```python from torch.optim.lr_scheduler import StepLR # Example usage with PyTorch scheduler = StepLR(optimizer, step_size=30, gamma=0.1) ``` These examples use PyTorch as the framework for illustration purposes."
    }
  ],
  "related_nodes": []
}