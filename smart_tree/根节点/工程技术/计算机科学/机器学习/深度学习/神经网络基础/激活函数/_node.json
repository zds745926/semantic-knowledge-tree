{
  "name": "激活函数",
  "node_id": "activation_v4",
  "parent_id": "nn_basics_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "神经网络基础",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "激活函数: ReLU(max(0, x)) is the most commonly used. Sigmoid, Tanh, and Softmax are also frequently employed. Here's a brief explanation of each with Python code examples: 1. **ReLU (Rectified Linear Unit)** - The ReLU function returns 0 if the input to the neuron is negative, and the input value itself if it is positive. ```python import numpy as np def relu(x): return np.maximum(0, x) # Example usage: print(relu(np.array([-1, 2, -3]))) # Output: [0 2 0] ``` 2. **Sigmoid** - The Sigmoid function squashes the input into a range between 0 and 1. ```python import numpy as np def sigmoid(x): return 1 / (1 + np.exp(-x)) # Example usage: print(sigmoid(np.array([-2, 0, 2]))) # Output: [9.52631578e-01 5.00000000e-01 1.10132327e-01] ``` 3. **Tanh (Hyperbolic Tangent)** - The Tanh function squashes the input into a range between -1 and 1. ```python import numpy as np def tanh(x): return np.tanh(x) # Example usage: print(tanh(np.array([-2, 0, 2]))) # Output: [-0.96402758 0. 0.96402758] ``` 4. **Softmax** - The Softmax function is used to turn logits into probabilities that sum up to 1. ```python import numpy as np def softmax(x): e_x = np.exp(x - np.max(x)) # Subtracting max for numerical stability return e_x / e_x.sum(axis=0) # Example usage: print(softmax(np.array([2.0, 1.0, 0.1]))) # Output: [0.65938746 0.24280926 0.09780328] ``` These functions are commonly used in neural networks for different purposes depending on the specific requirements of the model."
  },
  "data_pointers": [
    {
      "title": "激活函数",
      "uri": "knowledge://activation_v4",
      "content_preview": "激活函数: ReLU(max(0, x)) is the most commonly used. Sigmoid, Tanh, and Softmax are also frequently employed. Here's a brief explanation of each with Python code examples: 1. **ReLU (Rectified Linear Unit)** - The ReLU function returns 0 if the input to the neuron is negative, and the input value itself if it is positive. ```python import numpy as np def relu(x): return np.maximum(0, x) # Example usage: print(relu(np.array([-1, 2, -3]))) # Output: [0 2 0] ``` 2. **Sigmoid** - The Sigmoid function squashes the input into a range between 0 and 1. ```python import numpy as np def sigmoid(x): return 1 / (1 + np.exp(-x)) # Example usage: print(sigmoid(np.array([-2, 0, 2]))) # Output: [9.52631578e-01 5.00000000e-01 1.10132327e-01] ``` 3. **Tanh (Hyperbolic Tangent)** - The Tanh function squashes the input into a range between -1 and 1. ```python import numpy as np def tanh(x): return np.tanh(x) # Example usage: print(tanh(np.array([-2, 0, 2]))) # Output: [-0.96402758 0. 0.96402758] ``` 4. **Softmax** - The Softmax function is used to turn logits into probabilities that sum up to 1. ```python import numpy as np def softmax(x): e_x = np.exp(x - np.max(x)) # Subtracting max for numerical stability return e_x / e_x.sum(axis=0) # Example usage: print(softmax(np.array([2.0, 1.0, 0.1]))) # Output: [0.65938746 0.24280926 0.09780328] ``` These functions are commonly used in neural networks for different purposes depending on the specific requirements of the model."
    }
  ],
  "related_nodes": []
}