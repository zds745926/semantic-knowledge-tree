{
  "name": "逻辑回归",
  "node_id": "logistic_reg_v4",
  "parent_id": "regression_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "回归",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "逻辑回归: Sigmoid output probabilities. Cross-entropy loss. Standard algorithm for binary classification. Here is a brief explanation with a Python code example: 1. **Sigmoid Output Probabilities**: The Sigmoid function, often used in the final layer of binary classification models, outputs values between 0 and 1, which can be interpreted as probabilities. Here's how you might implement it in Python using NumPy: ```python import numpy as np def sigmoid(x): return 1 / (1 + np.exp(-x)) # Example usage: inputs = np.array([0.5, -0.2, 1.1]) probabilities = sigmoid(inputs) print(probabilities) # Output: [0.62245933 0.47502029 0.7521849 ] ``` 2. **Cross-Entropy Loss**: Cross-entropy loss is commonly used in classification tasks, especially when the output layer uses a Sigmoid function for binary classification or a Softmax function for multi-class classification. ```python import torch def binary_cross_entropy_loss(input, target): return -torch.mean(target * torch.log(input) + (1 - target) * torch.log(1 - input)) # Example usage: inputs = torch.tensor([0.5, 0.2, 0.8], requires_grad=True) targets = torch.tensor([1, 0, 1]) loss = binary_cross_entropy_loss(inputs, targets) print(loss.item()) # Output: 0.69314718 ``` 3. **Standard Algorithm for Binary Classification**: A standard algorithm for binary classification using a neural network typically involves the following steps: - Define a model with an input layer, one or more hidden layers, and an output layer. - Use the Sigmoid activation function in the output layer to produce probabilities. - Train the model using backpropagation and cross-entropy loss. Here's a simple example of such a neural network using PyTorch: ```python import torch.nn as nn class BinaryClassifier(nn.Module): def __init__(self, input_size): super(BinaryClassifier, self).__init__() self.layer = nn.Linear(input_size, 1) # One output neuron for binary classification def forward(self, x): return torch.sigmoid(self.layer(x)) # Example usage: model = BinaryClassifier(2) inputs = torch.tensor([[0.5, -0.2], [1.1, 0.3]]) outputs = model(inputs) print(outputs) # Output: tensor([[0.6224], [0.7522]], grad_fn=<SigmoidBackward>) ``` These examples should give you a clear understanding of how to implement and use these concepts in Python."
  },
  "data_pointers": [
    {
      "title": "逻辑回归",
      "uri": "knowledge://logistic_reg_v4",
      "content_preview": "逻辑回归: Sigmoid output probabilities. Cross-entropy loss. Standard algorithm for binary classification. Here is a brief explanation with a Python code example: 1. **Sigmoid Output Probabilities**: The Sigmoid function, often used in the final layer of binary classification models, outputs values between 0 and 1, which can be interpreted as probabilities. Here's how you might implement it in Python using NumPy: ```python import numpy as np def sigmoid(x): return 1 / (1 + np.exp(-x)) # Example usage: inputs = np.array([0.5, -0.2, 1.1]) probabilities = sigmoid(inputs) print(probabilities) # Output: [0.62245933 0.47502029 0.7521849 ] ``` 2. **Cross-Entropy Loss**: Cross-entropy loss is commonly used in classification tasks, especially when the output layer uses a Sigmoid function for binary classification or a Softmax function for multi-class classification. ```python import torch def binary_cross_entropy_loss(input, target): return -torch.mean(target * torch.log(input) + (1 - target) * torch.log(1 - input)) # Example usage: inputs = torch.tensor([0.5, 0.2, 0.8], requires_grad=True) targets = torch.tensor([1, 0, 1]) loss = binary_cross_entropy_loss(inputs, targets) print(loss.item()) # Output: 0.69314718 ``` 3. **Standard Algorithm for Binary Classification**: A standard algorithm for binary classification using a neural network typically involves the following steps: - Define a model with an input layer, one or more hidden layers, and an output layer. - Use the Sigmoid activation function in the output layer to produce probabilities. - Train the model using backpropagation and cross-entropy loss. Here's a simple example of such a neural network using PyTorch: ```python import torch.nn as nn class BinaryClassifier(nn.Module): def __init__(self, input_size): super(BinaryClassifier, self).__init__() self.layer = nn.Linear(input_size, 1) # One output neuron for binary classification def forward(self, x): return torch.sigmoid(self.layer(x)) # Example usage: model = BinaryClassifier(2) inputs = torch.tensor([[0.5, -0.2], [1.1, 0.3]]) outputs = model(inputs) print(outputs) # Output: tensor([[0.6224], [0.7522]], grad_fn=<SigmoidBackward>) ``` These examples should give you a clear understanding of how to implement and use these concepts in Python."
    }
  ],
  "related_nodes": []
}