{
  "name": "进程间通信 IPC",
  "node_id": "proc_comm_v4",
  "parent_id": "proc_v4",
  "level": 5,
  "is_leaf": false,
  "absorption_rate": 0.095,
  "version": 1,
  "parent": "进程与线程管理",
  "children": [
    "管道",
    "共享内存"
  ],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "进程间通信 IPC: Process communication methods include pipes, shared memory, semaphores, message queues, and sockets. Here are brief descriptions of each method along with a Python code example for one of them: 1. **Pipes**: Used to connect the standard output (stdout) of one process to the standard input (stdin) of another. ```python import os # Create a pipe r, w = os.pipe() # Write to the write end of the pipe os.write(w, b'Hello') # Close the write end os.close(w) # Read from the read end of the pipe data = os.read(r, 1024) print(data.decode('utf-8')) ``` 2. **Shared Memory**: Allows multiple processes to share a block of memory. ```python import mmap import os # Create a shared memory object fd = os.open(\"/tmp/my_shm\", os.O_RDWR | os.O_CREAT, 0o600) size = 1024 mm = mmap.mmap(fd, size) # Write to the shared memory mm.write(b'Hello') # Close and flush the shared memory mm.close() os.close(fd) ``` 3. **Semaphores**: Used for controlling access to a resource by multiple processes. ```python import threading semaphore = threading.Semaphore(2) # Allow up to 2 processes at a time def worker(): with semaphore: print(f\"Process {os.getpid()} is working\") threads = [threading.Thread(target=worker) for _ in range(5)] for t in threads: t.start() for t in threads: t.join() ``` 4. **Message Queues**: Used to send messages between processes. ```python import multiprocessing def sender(queue): queue.put(\"Hello from the sender\") def receiver(queue): print(f\"Received: {queue.get()}\") # Create a message queue q = multiprocessing.Queue() p1 = multiprocessing.Process(target=sender, args=(q,)) p2 = multiprocessing.Process(target=receiver, args=(q,)) p1.start() p2.start() p1.join() p2.join() ``` 5. **Sockets**: Used for network communication between processes. ```python import socket # Create a TCP/IP socket sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM) # Connect the socket to the port where the server is listening server_address = ('localhost', 10000) sock.connect(server_address) try: # Send data message = b'Hello, world' sock.sendall(message) # Look for the response amount_received = 0 amount_expected = len(message) while amount_received < amount_expected: data = sock.recv(16) amount_received += len(data) print(f\"Received: {data.decode('utf-8')}\") finally: sock.close() ```"
  },
  "data_pointers": [],
  "related_nodes": [],
  "summary": "进程间通信（IPC）是操作系统进程管理中的核心机制，旨在解决不同进程之间数据交换与同步协作的问题。本分支涵盖管道和共享内存两种经典IPC方式。管道是一种半双工通信方式，适用于有亲缘关系的进程（如父子进程），数据以字节流形式在内存中传递，实现简单但效率较低；共享内存则允许多个进程直接访问同一块物理内存区域，数据读写无需内核介入，因此通信速度极快，但需要借助信号量等同步机制避免竞争条件。两者在功能上互补：管道适用于小数据量、简单同步的场景，而共享内存适合大数据量、高性能要求的应用。学习路径建议先掌握管道的基本原理与系统调用（pipe、命名管道），理解其单向流动与阻塞特性；再深入共享内存的创建（shmget）、映射（shmat）与同步控制，最后通过对比两者的优缺点与适用场景，形成完整的IPC知识体系。",
  "example_queries": [
    "管道通信中，如何实现父子进程之间的双向数据传递？",
    "共享内存与管道相比，在性能和使用上有什么主要区别？",
    "使用共享内存时，如何用信号量解决进程间的同步问题？",
    "命名管道（FIFO）与匿名管道在应用场景上有何不同？",
    "在Linux系统中，如何通过系统调用创建并访问共享内存段？"
  ]
}