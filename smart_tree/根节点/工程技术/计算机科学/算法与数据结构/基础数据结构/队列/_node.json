{
  "name": "队列",
  "node_id": "queue_v4",
  "parent_id": "ds_basics_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "基础数据结构",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "队列: First-In-First-Out (FIFO). enqueue/dequeue operations are O(1). Applications: BFS/Breadth-First Search, task scheduling, message queues. Here's a brief Python code example for implementing a simple FIFO queue: ```python class Queue: def __init__(self): self.items = [] def is_empty(self): return not bool(self.items) def enqueue(self, item): self.items.append(item) def dequeue(self): if not self.is_empty(): return self.items.pop(0) else: raise IndexError(\"pop from empty queue\") def size(self): return len(self.items) # Example usage q = Queue() q.enqueue('first') q.enqueue('second') print(q.dequeue()) # Output: 'first' print(q.dequeue()) # Output: 'second' ``` This code defines a `Queue` class with methods to enqueue, dequeue items, check if the queue is empty, and get its size."
  },
  "data_pointers": [
    {
      "title": "队列",
      "uri": "knowledge://queue_v4",
      "content_preview": "队列: First-In-First-Out (FIFO). enqueue/dequeue operations are O(1). Applications: BFS/Breadth-First Search, task scheduling, message queues. Here's a brief Python code example for implementing a simple FIFO queue: ```python class Queue: def __init__(self): self.items = [] def is_empty(self): return not bool(self.items) def enqueue(self, item): self.items.append(item) def dequeue(self): if not self.is_empty(): return self.items.pop(0) else: raise IndexError(\"pop from empty queue\") def size(self): return len(self.items) # Example usage q = Queue() q.enqueue('first') q.enqueue('second') print(q.dequeue()) # Output: 'first' print(q.dequeue()) # Output: 'second' ``` This code defines a `Queue` class with methods to enqueue, dequeue items, check if the queue is empty, and get its size."
    }
  ],
  "related_nodes": [
    {
      "id": "py_lifo_queue_v4",
      "type": "contrast"
    },
    {
      "id": "py_collections_module_v4",
      "type": "related_pattern"
    },
    {
      "id": "py_thread_safety_v4",
      "type": "application"
    }
  ],
  "content": {
    "preview": "",
    "full": "## 定义\n队列是一种先进先出（FIFO, First In First Out）的线性数据结构，它只允许在表的一端（称为队尾）进行插入操作，在另一端（称为队头）进行删除操作。这种结构类似于现实生活中的排队场景，先进入队列的元素将最先被取出。\n\n## 核心要点\n- 先进先出原则：队列的核心特性是元素按照插入顺序被处理，最早插入的元素最先被移除。\n- 基本操作：主要包括入队（enqueue，在队尾添加元素）和出队（dequeue，从队头移除元素）两种操作。\n- 队头与队尾：队头指向第一个元素，队尾指向最后一个元素的下一个位置，用于控制插入和删除的位置。\n- 存储实现：队列可以用数组或链表实现，数组实现时需注意循环队列以避免空间浪费。\n- 时间复杂度：入队和出队操作的时间复杂度均为 O(1)，效率高。\n\n## 应用场景\n队列广泛应用于需要按顺序处理任务的场景，例如操作系统中的任务调度、打印机任务队列、消息队列系统（如 RabbitMQ、Kafka）以及广度优先搜索（BFS）算法中。此外，在 Web 服务器中，队列用于管理请求的排队处理，确保请求按到达顺序被响应。\n\n## 相关概念\n队列与栈（后进先出）形成对比，两者都是线性数据结构但顺序规则不同。双端队列（Deque）是队列的扩展，允许在两端进行插入和删除操作。优先队列则按优先级而非插入顺序处理元素，常基于堆实现。循环队列是数组实现队列的一种优化形式，通过循环利用空间避免假溢出。",
    "code_pattern": "",
    "common_pitfalls": []
  },
  "quality": {
    "confidence": 0.7,
    "version": 1,
    "reviewers": [],
    "last_updated": "2026-07-14"
  }
}