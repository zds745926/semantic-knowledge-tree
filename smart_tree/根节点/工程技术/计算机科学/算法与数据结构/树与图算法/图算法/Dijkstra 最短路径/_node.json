{
  "name": "Dijkstra 最短路径",
  "node_id": "dijkstra_v4",
  "parent_id": "graph_algo_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "图算法",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "Dijkstra 最短路径: Single-source shortest path (non-negative weights). Greedy + priority queue. O(E log V). Navigation core. Here is a brief explanation with a Python code example for the single-source shortest path algorithm using Dijkstra's method, which fits this description: ```python import heapq def dijkstra(graph, start): # Initialize distances to all nodes as infinity and set the distance of the start node to 0 distances = {node: float('infinity') for node in graph} distances[start] = 0 # Priority queue to store (distance, node) tuples priority_queue = [(0, start)] while priority_queue: # Pop the node with the smallest distance from the priority queue current_distance, current_node = heapq.heappop(priority_queue) # If the current distance is greater than the recorded distance, skip this iteration if current_distance > distances[current_node]: continue # Explore neighbors of the current node for neighbor, weight in graph[current_node].items(): distance = current_distance + weight # Only consider this new path if it's better (shorter) if distance < distances[neighbor]: distances[neighbor] = distance heapq.heappush(priority_queue, (distance, neighbor)) return distances # Example graph represented as an adjacency list with non-negative weights graph = { 'A': {'B': 1, 'C': 4}, 'B': {'A': 1, 'C': 2, 'D': 5}, 'C': {'A': 4, 'B': 2, 'D': 1}, 'D': {'B': 5, 'C': 1} } # Find the shortest paths from node 'A' shortest_paths = dijkstra(graph, 'A') print(shortest_paths) ``` This Python code implements Dijkstra's algorithm to find the shortest paths from a single source (node `A` in this example) to all other nodes in a graph with non-negative edge weights. The use of a priority queue ensures that the node with the smallest known distance is processed next, which aligns with the greedy approach described in the original text."
  },
  "data_pointers": [
    {
      "title": "Dijkstra 最短路径",
      "uri": "knowledge://dijkstra_v4",
      "content_preview": "Dijkstra 最短路径: Single-source shortest path (non-negative weights). Greedy + priority queue. O(E log V). Navigation core. Here is a brief explanation with a Python code example for the single-source shortest path algorithm using Dijkstra's method, which fits this description: ```python import heapq def dijkstra(graph, start): # Initialize distances to all nodes as infinity and set the distance of the start node to 0 distances = {node: float('infinity') for node in graph} distances[start] = 0 # Priority queue to store (distance, node) tuples priority_queue = [(0, start)] while priority_queue: # Pop the node with the smallest distance from the priority queue current_distance, current_node = heapq.heappop(priority_queue) # If the current distance is greater than the recorded distance, skip this iteration if current_distance > distances[current_node]: continue # Explore neighbors of the current node for neighbor, weight in graph[current_node].items(): distance = current_distance + weight # Only consider this new path if it's better (shorter) if distance < distances[neighbor]: distances[neighbor] = distance heapq.heappush(priority_queue, (distance, neighbor)) return distances # Example graph represented as an adjacency list with non-negative weights graph = { 'A': {'B': 1, 'C': 4}, 'B': {'A': 1, 'C': 2, 'D': 5}, 'C': {'A': 4, 'B': 2, 'D': 1}, 'D': {'B': 5, 'C': 1} } # Find the shortest paths from node 'A' shortest_paths = dijkstra(graph, 'A') print(shortest_paths) ``` This Python code implements Dijkstra's algorithm to find the shortest paths from a single source (node `A` in this example) to all other nodes in a graph with non-negative edge weights. The use of a priority queue ensures that the node with the smallest known distance is processed next, which aligns with the greedy approach described in the original text."
    }
  ],
  "related_nodes": [
    "floyd_v4"
  ],
  "content": {
    "preview": "",
    "full": "## 定义\nDijkstra 最短路径算法是一种用于计算图中单源最短路径的经典算法，适用于边权非负的图。它通过贪心策略，从起点开始逐步扩展最短路径树，直到到达所有其他节点。\n\n## 核心要点\n- 算法维护一个距离数组，记录从起点到各节点的当前最短距离，初始时起点为0，其余为无穷大。\n- 每次从未处理的节点中选择距离最小的节点，标记为已处理，并更新其邻接节点的距离。\n- 使用优先队列（如最小堆）可以高效地选择当前最小距离节点，将时间复杂度优化至 O((V+E)logV)。\n- 算法要求图中所有边的权重为非负值，否则无法保证正确性。\n- 最终得到从起点到所有其他节点的最短路径长度，且路径可通过前驱节点数组回溯。\n\n## 应用场景\nDijkstra 算法广泛应用于计算机网络中的路由协议（如 OSPF）、地图导航系统中的路径规划、以及交通网络中的最短路线计算。此外，在机器人路径规划、通信网络优化等领域也有重要应用。\n\n## 相关概念\nDijkstra 算法与广度优先搜索（BFS）类似，但 BFS 用于无权图，而 Dijkstra 处理带权图。与 Bellman-Ford 算法相比，Dijkstra 效率更高但不能处理负权边。最小生成树算法（如 Prim 算法）在结构上与 Dijkstra 相似，但目标不同。",
    "code_pattern": "",
    "common_pitfalls": []
  },
  "quality": {
    "confidence": 0.7,
    "version": 1,
    "reviewers": [],
    "last_updated": "2026-07-14"
  }
}