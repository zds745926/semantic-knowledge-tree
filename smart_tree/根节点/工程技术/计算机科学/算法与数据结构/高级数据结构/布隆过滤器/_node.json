{
  "name": "布隆过滤器",
  "node_id": "bloom_filter_v4",
  "parent_id": "adv_ds_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "高级数据结构",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "布隆过滤器: Bit array + multiple hash. Determine if something is 'definitely not' or 'possibly in'. Prevents cache penetration/de-duplication. Here's a brief explanation with Python code examples for the concepts mentioned: 1. **Bit Array**: A bit array is a space-efficient data structure that holds a sequence of bits (0s and 1s). It can be used to track the presence or absence of elements in a set. ```python # Example: Using a bit array to check if an element exists bit_array = [False] * 1024 # Initialize a bit array of size 1024 def add_element(index): \"\"\"Add an element by setting the corresponding bit.\"\"\" bit_array[index] = True def is_element_present(index): \"\"\"Check if the element is present by checking the corresponding bit.\"\"\" return bit_array[index] # Usage add_element(500) print(is_element_present(500)) # Output: True ``` 2. **Multiple Hash**: Using multiple hash functions can help distribute data more evenly and reduce collisions, which is useful in scenarios like cache penetration prevention. ```python import hashlib def hash_function1(key): \"\"\"Simple hash function using SHA-256.\"\"\" return int(hashlib.sha256(str(key).encode()).hexdigest(), 16) % 1024 def hash_function2(key): \"\"\"Another simple hash function.\"\"\" return sum(ord(c) for c in str(key)) % 1024 # Example: Using multiple hashes to check if a key is present keys = [hash_function1('key1'), hash_function2('key1')] print(keys) # Output will vary based on the input and hashing functions used ``` 3. **Prevent Cache Penetration**: Cache penetration occurs when a cache miss happens due to a request that should have hit the cache but didn't because of some issue, such as incorrect configuration or data corruption. ```python from functools import lru_cache @lru_cache(maxsize=128) def get_data(key): \"\"\"Simulate getting data from a database.\"\"\" print(f\"Fetching data for key: {key}\") return f\"data for {key}\" # Usage print(get_data('key1')) # Output: Fetching data for key: key1, followed by the cached result on subsequent calls ``` 4. **De-duplication**: To prevent duplicate entries in a cache or database. ```python seen = set() def process_item(item): \"\"\"Process an item and avoid duplicates.\"\"\" if item not in seen: seen.add(item) # Process the item here # Example usage items = ['item1', 'item2', 'item1', 'item3'] for item in items: process_item(item) print(seen) # Output: {'item1', 'item2', 'item3'} ``` These examples should help illustrate the concepts and provide a practical understanding of how they can be implemented in Python."
  },
  "data_pointers": [
    {
      "title": "布隆过滤器",
      "uri": "knowledge://bloom_filter_v4",
      "content_preview": "布隆过滤器: Bit array + multiple hash. Determine if something is 'definitely not' or 'possibly in'. Prevents cache penetration/de-duplication. Here's a brief explanation with Python code examples for the concepts mentioned: 1. **Bit Array**: A bit array is a space-efficient data structure that holds a sequence of bits (0s and 1s). It can be used to track the presence or absence of elements in a set. ```python # Example: Using a bit array to check if an element exists bit_array = [False] * 1024 # Initialize a bit array of size 1024 def add_element(index): \"\"\"Add an element by setting the corresponding bit.\"\"\" bit_array[index] = True def is_element_present(index): \"\"\"Check if the element is present by checking the corresponding bit.\"\"\" return bit_array[index] # Usage add_element(500) print(is_element_present(500)) # Output: True ``` 2. **Multiple Hash**: Using multiple hash functions can help distribute data more evenly and reduce collisions, which is useful in scenarios like cache penetration prevention. ```python import hashlib def hash_function1(key): \"\"\"Simple hash function using SHA-256.\"\"\" return int(hashlib.sha256(str(key).encode()).hexdigest(), 16) % 1024 def hash_function2(key): \"\"\"Another simple hash function.\"\"\" return sum(ord(c) for c in str(key)) % 1024 # Example: Using multiple hashes to check if a key is present keys = [hash_function1('key1'), hash_function2('key1')] print(keys) # Output will vary based on the input and hashing functions used ``` 3. **Prevent Cache Penetration**: Cache penetration occurs when a cache miss happens due to a request that should have hit the cache but didn't because of some issue, such as incorrect configuration or data corruption. ```python from functools import lru_cache @lru_cache(maxsize=128) def get_data(key): \"\"\"Simulate getting data from a database.\"\"\" print(f\"Fetching data for key: {key}\") return f\"data for {key}\" # Usage print(get_data('key1')) # Output: Fetching data for key: key1, followed by the cached result on subsequent calls ``` 4. **De-duplication**: To prevent duplicate entries in a cache or database. ```python seen = set() def process_item(item): \"\"\"Process an item and avoid duplicates.\"\"\" if item not in seen: seen.add(item) # Process the item here # Example usage items = ['item1', 'item2', 'item1', 'item3'] for item in items: process_item(item) print(seen) # Output: {'item1', 'item2', 'item3'} ``` These examples should help illustrate the concepts and provide a practical understanding of how they can be implemented in Python."
    }
  ],
  "related_nodes": []
}