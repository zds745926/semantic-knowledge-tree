{
  "name": "深度优先搜索 DFS",
  "node_id": "dfs_v4",
  "parent_id": "search_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "搜索算法",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "深度优先搜索 DFS: Recursive calls or stack overflow until backtracking. O(V+E). Applications: Connected Components / Topological Sorting. Here are brief Python code examples for the applications mentioned: ### Connected Components in a Graph ```python def dfs(graph, v, visited): visited[v] = True for neighbor in graph[v]: if not visited[neighbor]: dfs(graph, neighbor, visited) def find_connected_components(graph): num_nodes = len(graph) visited = [False] * num_nodes components = [] for node in range(num_nodes): if not visited[node]: component = [] dfs(graph, node, visited) components.append(component) return components # Example graph represented as an adjacency list graph = { 0: [1, 2], 1: [2], 2: [0, 3], 3: [3] } print(find_connected_components(graph)) ``` ### Topological Sorting of a Directed Acyclic Graph (DAG) ```python from collections import defaultdict def dfs_topo_sort(graph, node, visited, stack): visited[node] = True for neighbor in graph[node]: if not visited[neighbor]: dfs_topo_sort(graph, neighbor, visited, stack) stack.append(node) def topological_sort(graph): num_nodes = len(graph) visited = [False] * num_nodes stack = [] for node in range(num_nodes): if not visited[node]: dfs_topo_sort(graph, node, visited, stack) return stack[::-1] # Example graph represented as an adjacency list graph = { 0: [1], 1: [2], 2: [] } print(topological_sort(graph)) ``` These examples illustrate how to implement the concepts of connected components and topological sorting in Python."
  },
  "data_pointers": [
    {
      "title": "深度优先搜索 DFS",
      "uri": "knowledge://dfs_v4",
      "content_preview": "深度优先搜索 DFS: Recursive calls or stack overflow until backtracking. O(V+E). Applications: Connected Components / Topological Sorting. Here are brief Python code examples for the applications mentioned: ### Connected Components in a Graph ```python def dfs(graph, v, visited): visited[v] = True for neighbor in graph[v]: if not visited[neighbor]: dfs(graph, neighbor, visited) def find_connected_components(graph): num_nodes = len(graph) visited = [False] * num_nodes components = [] for node in range(num_nodes): if not visited[node]: component = [] dfs(graph, node, visited) components.append(component) return components # Example graph represented as an adjacency list graph = { 0: [1, 2], 1: [2], 2: [0, 3], 3: [3] } print(find_connected_components(graph)) ``` ### Topological Sorting of a Directed Acyclic Graph (DAG) ```python from collections import defaultdict def dfs_topo_sort(graph, node, visited, stack): visited[node] = True for neighbor in graph[node]: if not visited[neighbor]: dfs_topo_sort(graph, neighbor, visited, stack) stack.append(node) def topological_sort(graph): num_nodes = len(graph) visited = [False] * num_nodes stack = [] for node in range(num_nodes): if not visited[node]: dfs_topo_sort(graph, node, visited, stack) return stack[::-1] # Example graph represented as an adjacency list graph = { 0: [1], 1: [2], 2: [] } print(topological_sort(graph)) ``` These examples illustrate how to implement the concepts of connected components and topological sorting in Python."
    }
  ],
  "related_nodes": []
}