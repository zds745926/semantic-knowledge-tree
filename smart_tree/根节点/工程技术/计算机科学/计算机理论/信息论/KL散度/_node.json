{
  "name": "KL散度",
  "node_id": "kl_divergence_v4",
  "parent_id": "info_theory_v4",
  "level": 5,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "信息论",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "KL散度: KL Divergence (Kullback-Leibler Divergence): \\(D_{KL}(P \\parallel Q) = \\sum P(x) \\log \\left(\\frac{P(x)}{Q(x)}\\right)\\) This measures the difference between two probability distributions, and it is not symmetric. Python code example: ```python import numpy as np def kl_divergence(p, q): return np.sum(np.where(p != 0, p * np.log(p / q), 0)) # Example usage p = np.array([0.2, 0.3, 0.5]) q = np.array([0.1, 0.4, 0.5]) print(kl_divergence(p, q)) ``` This Python function calculates the KL divergence between two probability distributions `p` and `q`. Note that the KL divergence is not symmetric, meaning \\(D_{KL}(P \\parallel Q) \\neq D_{KL}(Q \\parallel P)\\)."
  },
  "data_pointers": [
    {
      "title": "KL散度",
      "uri": "knowledge://kl_divergence_v4",
      "content_preview": "KL散度: KL Divergence (Kullback-Leibler Divergence): \\(D_{KL}(P \\parallel Q) = \\sum P(x) \\log \\left(\\frac{P(x)}{Q(x)}\\right)\\) This measures the difference between two probability distributions, and it is not symmetric. Python code example: ```python import numpy as np def kl_divergence(p, q): return np.sum(np.where(p != 0, p * np.log(p / q), 0)) # Example usage p = np.array([0.2, 0.3, 0.5]) q = np.array([0.1, 0.4, 0.5]) print(kl_divergence(p, q)) ``` This Python function calculates the KL divergence between two probability distributions `p` and `q`. Note that the KL divergence is not symmetric, meaning \\(D_{KL}(P \\parallel Q) \\neq D_{KL}(Q \\parallel P)\\)."
    }
  ],
  "related_nodes": [
    {
      "id": "py_str_bytes_encode_v4",
      "type": "prerequisite"
    }
  ],
  "content": {
    "preview": "",
    "full": "## 定义\nKL散度（Kullback-Leibler divergence），又称相对熵，是衡量两个概率分布P和Q之间差异的非对称性度量。它定义为从Q到P的信息损失，即用Q近似P时所需的额外信息量。\n\n## 核心要点\n- 非对称性：KL(P||Q)通常不等于KL(Q||P)，因此它不是真正的距离度量。\n- 非负性：KL散度始终大于等于0，当且仅当P和Q完全相同时为0。\n- 信息论解释：KL散度表示使用Q编码来自P的样本时，平均所需的额外比特数。\n- 计算方式：对于离散分布，KL(P||Q)=∑P(x)log(P(x)/Q(x))；对于连续分布，则为积分形式。\n\n## 应用场景\nKL散度广泛应用于机器学习中的模型优化，如变分自编码器（VAE）的损失函数、生成对抗网络（GAN）的稳定性分析，以及自然语言处理中的主题模型评估。它还用于特征选择、异常检测和强化学习中的策略评估。\n\n## 相关概念\n- 交叉熵：KL散度等于交叉熵减去信息熵，常用于分类任务的损失函数。\n- 詹森-香农散度：基于KL散度的对称化版本，用于衡量分布间的平衡差异。\n- 互信息：两个变量联合分布与边缘分布乘积的KL散度，衡量变量间的依赖关系。",
    "code_pattern": "",
    "common_pitfalls": []
  },
  "quality": {
    "confidence": 0.7,
    "version": 1,
    "reviewers": [],
    "last_updated": "2026-07-14"
  }
}