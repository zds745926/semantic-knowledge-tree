{
  "name": "DML 数据操作",
  "node_id": "dml_v4",
  "parent_id": "sql_v4",
  "level": 6,
  "is_leaf": true,
  "absorption_rate": 0.2,
  "version": 1,
  "parent": "SQL 语言",
  "children": [],
  "vector_file": "vector.txt",
  "metadata": {
    "description": "DML 数据操作: SELECT/INSERT/UPDATE/DELETE. WHERE/ORDER BY/LIMIT. In the context of SQL (Structured Query Language), these are fundamental commands used for managing data in relational databases. Here's a brief explanation and example using Python with the SQLite3 library: 1. **SELECT**: Used to retrieve data from one or more tables. 2. **INSERT**: Adds new records to a table. 3. **UPDATE**: Modifies existing records in a table. 4. **DELETE**: Removes records from a table. 5. **WHERE**: Filters records based on specified conditions. 6. **ORDER BY**: Sorts the result-set in ascending or descending order. 7. **LIMIT**: Limits the number of records returned. ### Python Example with SQLite3 ```python import sqlite3 # Connect to SQLite database (or create it if it doesn't exist) conn = sqlite3.connect('example.db') cursor = conn.cursor() # Create a table for demonstration purposes cursor.execute(''' CREATE TABLE IF NOT EXISTS employees ( id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, department TEXT ) ''') # INSERT example cursor.execute(\"INSERT INTO employees (name, age, department) VALUES ('Alice', 30, 'HR')\") conn.commit() # SELECT example cursor.execute(\"SELECT * FROM employees\") print(cursor.fetchall()) # UPDATE example cursor.execute(\"UPDATE employees SET age = 31 WHERE name = 'Alice'\") conn.commit() # DELETE example cursor.execute(\"DELETE FROM employees WHERE id = 1\") conn.commit() # ORDER BY and LIMIT examples cursor.execute(\"SELECT * FROM employees ORDER BY age DESC LIMIT 2\") print(cursor.fetchall()) # Close the connection conn.close() ``` This Python script demonstrates basic SQL operations using SQLite3, including creating a table, inserting data, updating records, deleting records, and querying with `ORDER BY` and `LIMIT`."
  },
  "data_pointers": [
    {
      "title": "DML 数据操作",
      "uri": "knowledge://dml_v4",
      "content_preview": "DML 数据操作: SELECT/INSERT/UPDATE/DELETE. WHERE/ORDER BY/LIMIT. In the context of SQL (Structured Query Language), these are fundamental commands used for managing data in relational databases. Here's a brief explanation and example using Python with the SQLite3 library: 1. **SELECT**: Used to retrieve data from one or more tables. 2. **INSERT**: Adds new records to a table. 3. **UPDATE**: Modifies existing records in a table. 4. **DELETE**: Removes records from a table. 5. **WHERE**: Filters records based on specified conditions. 6. **ORDER BY**: Sorts the result-set in ascending or descending order. 7. **LIMIT**: Limits the number of records returned. ### Python Example with SQLite3 ```python import sqlite3 # Connect to SQLite database (or create it if it doesn't exist) conn = sqlite3.connect('example.db') cursor = conn.cursor() # Create a table for demonstration purposes cursor.execute(''' CREATE TABLE IF NOT EXISTS employees ( id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, department TEXT ) ''') # INSERT example cursor.execute(\"INSERT INTO employees (name, age, department) VALUES ('Alice', 30, 'HR')\") conn.commit() # SELECT example cursor.execute(\"SELECT * FROM employees\") print(cursor.fetchall()) # UPDATE example cursor.execute(\"UPDATE employees SET age = 31 WHERE name = 'Alice'\") conn.commit() # DELETE example cursor.execute(\"DELETE FROM employees WHERE id = 1\") conn.commit() # ORDER BY and LIMIT examples cursor.execute(\"SELECT * FROM employees ORDER BY age DESC LIMIT 2\") print(cursor.fetchall()) # Close the connection conn.close() ``` This Python script demonstrates basic SQL operations using SQLite3, including creating a table, inserting data, updating records, deleting records, and querying with `ORDER BY` and `LIMIT`."
    }
  ],
  "related_nodes": []
}